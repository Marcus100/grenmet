{
  "properties": {
    "entry_date": { "type": "string", "format": "date", "title": "Entry Date" },
    "shift_code": {
      "anyOf": [{ "type": "string" }, { "type": "null" }],
      "title": "Shift Code"
    },
    "roster_hours": {
      "anyOf": [
        { "type": "number" },
        { "type": "string", "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$" }
      ],
      "title": "Roster Hours",
      "default": "0.0"
    },
    "actual_hours": {
      "anyOf": [
        { "type": "number" },
        { "type": "string", "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$" }
      ],
      "title": "Actual Hours",
      "default": "0.0"
    },
    "total_hours": {
      "anyOf": [
        { "type": "number" },
        { "type": "string", "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$" },
        { "type": "null" }
      ],
      "title": "Total Hours"
    },
    "overtime_hours": {
      "anyOf": [
        { "type": "number" },
        { "type": "string", "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$" }
      ],
      "title": "Overtime Hours",
      "default": "0.0"
    },
    "break_hours": {
      "anyOf": [
        { "type": "number" },
        { "type": "string", "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$" }
      ],
      "title": "Break Hours",
      "default": "0.0"
    },
    "hours_worked": {
      "anyOf": [
        { "type": "number" },
        { "type": "string", "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$" },
        { "type": "null" }
      ],
      "title": "Hours Worked"
    },
    "medical_certificate_attached": {
      "type": "boolean",
      "title": "Medical Certificate Attached",
      "default": false
    },
    "comments": {
      "anyOf": [{ "type": "string" }, { "type": "null" }],
      "title": "Comments"
    }
  },
  "type": "object",
  "required": ["entry_date"],
  "title": "TimesheetEntryInput",
  "x-readme-ref-name": "TimesheetEntryInput"
}
