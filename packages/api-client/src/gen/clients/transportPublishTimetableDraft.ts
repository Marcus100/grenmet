/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Options, RequestResult, Unwrappable } from "../.kubb/client.js";
import { client, withUnwrap } from "../.kubb/client.js";
import type {
  TransportPublishTimetableDraftOptions,
  TransportPublishTimetableDraftResponses,
} from "../models/TransportPublishTimetableDraft.js";

/**
 * @description Validates the draft and publishes it from the effective date (today or later). Blocking issues return 422.
 * @summary Publish a timetable draft
 * {@link /api/v1/transport/timetable/versions/:version_id/publish}
 */
export function transportPublishTimetableDraft<
  ThrowOnError extends boolean = true,
>(
  options: Options<TransportPublishTimetableDraftOptions, ThrowOnError>
): Unwrappable<
  RequestResult<TransportPublishTimetableDraftResponses, ThrowOnError>
> {
  const { client: request = client, ...config } = options;

  return withUnwrap(
    request({
      method: "POST",
      url: "/api/v1/transport/timetable/versions/{version_id}/publish",
      security: [
        { type: "apiKey", name: "grenmet_session", in: "cookie" },
        { type: "http", scheme: "bearer" },
      ],
      ...config,
    }) as Promise<
      RequestResult<TransportPublishTimetableDraftResponses, ThrowOnError>
    >
  );
}
