/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Options, RequestResult, Unwrappable } from "../.kubb/client.js";
import { client, withUnwrap } from "../.kubb/client.js";
import type {
  TransportGetAccessOptions,
  TransportGetAccessResponses,
} from "../models/TransportGetAccess.js";

/**
 * @description Returns which transport portal actions the signed-in user may take, so clients can show or hide controls. The API still enforces every permission.
 * @summary Get my transport permissions
 * {@link /api/v1/transport/access}
 */
export function transportGetAccess<ThrowOnError extends boolean = true>(
  options: Options<TransportGetAccessOptions, ThrowOnError> = {}
): Unwrappable<RequestResult<TransportGetAccessResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options;

  return withUnwrap(
    request({
      method: "GET",
      url: "/api/v1/transport/access",
      security: [
        { type: "apiKey", name: "grenmet_session", in: "cookie" },
        { type: "http", scheme: "bearer" },
      ],
      ...config,
    }) as Promise<RequestResult<TransportGetAccessResponses, ThrowOnError>>
  );
}
