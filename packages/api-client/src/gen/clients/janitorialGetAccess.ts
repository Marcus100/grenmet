/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Options, RequestResult, Unwrappable } from "../.kubb/client.js";
import { client, withUnwrap } from "../.kubb/client.js";
import type {
  JanitorialGetAccessOptions,
  JanitorialGetAccessResponses,
} from "../models/JanitorialGetAccess.js";

/**
 * @description Returns which janitorial portal actions the signed-in user may take and the buildings they may act on, so clients can show or hide controls. The API still enforces every rule.
 * @summary Get my janitorial permissions
 * {@link /api/v1/janitorial/access}
 */
export function janitorialGetAccess<ThrowOnError extends boolean = true>(
  options: Options<JanitorialGetAccessOptions, ThrowOnError> = {}
): Unwrappable<RequestResult<JanitorialGetAccessResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options;

  return withUnwrap(
    request({
      method: "GET",
      url: "/api/v1/janitorial/access",
      security: [
        { type: "apiKey", name: "grenmet_session", in: "cookie" },
        { type: "http", scheme: "bearer" },
      ],
      ...config,
    }) as Promise<RequestResult<JanitorialGetAccessResponses, ThrowOnError>>
  );
}
