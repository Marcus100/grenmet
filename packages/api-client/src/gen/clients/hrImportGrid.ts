/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Options, RequestResult, Unwrappable } from "../.kubb/client.js";
import { client, withUnwrap } from "../.kubb/client.js";
import type {
  HrImportGridOptions,
  HrImportGridResponses,
} from "../models/HrImportGrid.js";

/**
 * @description Import a name×day grid CSV into a draft roster period (created or reused for the month). Blocked if any name is unmatched or any code invalid. Requires roster.manage permission.
 * @summary Import a grid roster
 * {@link /api/v1/hr/rosters/import-grid}
 */
export function hrImportGrid<ThrowOnError extends boolean = true>(
  options: Options<HrImportGridOptions, ThrowOnError>
): Unwrappable<RequestResult<HrImportGridResponses, ThrowOnError>> {
  const { client: request = client, ...config } = options;

  return withUnwrap(
    request({
      method: "POST",
      url: "/api/v1/hr/rosters/import-grid",
      security: [{ type: "oauth2" }],
      ...config,
    }) as Promise<RequestResult<HrImportGridResponses, ThrowOnError>>
  );
}
