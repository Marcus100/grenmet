/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Options, RequestResult, Unwrappable } from "../.kubb/client.js";
import { client, withUnwrap } from "../.kubb/client.js";
import type {
  TransportReplaceTimetableTripOptions,
  TransportReplaceTimetableTripResponses,
} from "../models/TransportReplaceTimetableTrip.js";

/**
 * @description Replaces the trip's details and its full ordered stop list.
 * @summary Replace a trip in a draft
 * {@link /api/v1/transport/timetable/versions/:version_id/trips/:trip_id}
 */
export function transportReplaceTimetableTrip<
  ThrowOnError extends boolean = true,
>(
  options: Options<TransportReplaceTimetableTripOptions, ThrowOnError>
): Unwrappable<
  RequestResult<TransportReplaceTimetableTripResponses, ThrowOnError>
> {
  const { client: request = client, ...config } = options;

  return withUnwrap(
    request({
      method: "PUT",
      url: "/api/v1/transport/timetable/versions/{version_id}/trips/{trip_id}",
      security: [
        { type: "apiKey", name: "grenmet_session", in: "cookie" },
        { type: "http", scheme: "bearer" },
      ],
      ...config,
    }) as Promise<
      RequestResult<TransportReplaceTimetableTripResponses, ThrowOnError>
    >
  );
}
