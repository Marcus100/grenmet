/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Options, RequestResult, Unwrappable } from "../.kubb/client.js";
import { client, withUnwrap } from "../.kubb/client.js";
import type {
  TransportGetCurrentTimetableOptions,
  TransportGetCurrentTimetableResponses,
} from "../models/TransportGetCurrentTimetable.js";

/**
 * @description Returns the published timetable in force today (Grenada time) with its trips and stop times. Any signed-in user may read it.
 * @summary Get the timetable in force
 * {@link /api/v1/transport/timetable/current}
 */
export function transportGetCurrentTimetable<
  ThrowOnError extends boolean = true,
>(
  options: Options<TransportGetCurrentTimetableOptions, ThrowOnError> = {}
): Unwrappable<
  RequestResult<TransportGetCurrentTimetableResponses, ThrowOnError>
> {
  const { client: request = client, ...config } = options;

  return withUnwrap(
    request({
      method: "GET",
      url: "/api/v1/transport/timetable/current",
      security: [
        { type: "apiKey", name: "grenmet_session", in: "cookie" },
        { type: "http", scheme: "bearer" },
      ],
      ...config,
    }) as Promise<
      RequestResult<TransportGetCurrentTimetableResponses, ThrowOnError>
    >
  );
}
