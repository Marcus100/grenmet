/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Options, RequestResult, Unwrappable } from "../.kubb/client.js";
import { client, withUnwrap } from "../.kubb/client.js";
import type {
  TransportListTimetableVersionsOptions,
  TransportListTimetableVersionsResponses,
} from "../models/TransportListTimetableVersions.js";

/**
 * @description Returns every timetable version, newest first, with its lifecycle state (draft, scheduled, current, superseded, discarded).
 * @summary List timetable versions
 * {@link /api/v1/transport/timetable/versions}
 */
export function transportListTimetableVersions<
  ThrowOnError extends boolean = true,
>(
  options: Options<TransportListTimetableVersionsOptions, ThrowOnError> = {}
): Unwrappable<
  RequestResult<TransportListTimetableVersionsResponses, ThrowOnError>
> {
  const { client: request = client, ...config } = options;

  return withUnwrap(
    request({
      method: "GET",
      url: "/api/v1/transport/timetable/versions",
      security: [
        { type: "apiKey", name: "grenmet_session", in: "cookie" },
        { type: "http", scheme: "bearer" },
      ],
      ...config,
    }) as Promise<
      RequestResult<TransportListTimetableVersionsResponses, ThrowOnError>
    >
  );
}
