/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import * as z from "zod";

export const notificationParamsSchema = z
  .object({
    remind_after_days: z.union([z.int().min(1).max(365), z.null()]).optional(),
    escalate_after_days: z
      .union([z.int().min(1).max(365), z.null()])
      .optional(),
    expiry_days_before: z
      .union([z.array(z.int()).min(1).max(5), z.null()])
      .optional(),
  })
  .describe(
    "Tunable timings. Each event uses only the ones it lists as defaults."
  );
