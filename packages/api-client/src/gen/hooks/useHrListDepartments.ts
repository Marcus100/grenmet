/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { hrListDepartments } from "../clients/hrListDepartments.js";
import type {
  HrListDepartmentsOptions,
  HrListDepartmentsStatus200,
  HrListDepartmentsStatus403,
  HrListDepartmentsStatus422,
} from "../models/HrListDepartments.js";

export const hrListDepartmentsQueryKey = ({
  query,
}: Omit<HrListDepartmentsOptions, "headers"> = {}) =>
  [{ url: "/api/v1/hr/departments" }, ...(query ? [query] : [])] as const;

type HrListDepartmentsQueryKey = ReturnType<typeof hrListDepartmentsQueryKey>;

export function hrListDepartmentsQueryOptions(
  { query }: HrListDepartmentsOptions = {},
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = hrListDepartmentsQueryKey({ query });
  return queryOptions<
    HrListDepartmentsStatus200,
    ResponseErrorConfig<
      HrListDepartmentsStatus403 | HrListDepartmentsStatus422
    >,
    HrListDepartmentsStatus200,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return hrListDepartments({
        ...config,
        query,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Return all departments, ordered by name. Requires roster.view permission.
 * @summary List departments
 * {@link /api/v1/hr/departments}
 */
export function useHrListDepartments<
  TData = HrListDepartmentsStatus200,
  TQueryData = HrListDepartmentsStatus200,
  TQueryKey extends QueryKey = HrListDepartmentsQueryKey,
>(
  {
    query,
  }: {
    query?:
      | HrListDepartmentsOptions["query"]
      | (() => HrListDepartmentsOptions["query"]);
  } = {},
  options: {
    query?: Partial<
      QueryObserverOptions<
        HrListDepartmentsStatus200,
        ResponseErrorConfig<
          HrListDepartmentsStatus403 | HrListDepartmentsStatus422
        >,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const resolvedParams = {
    query: typeof query === "function" ? query() : query,
  };
  const queryKey =
    resolvedOptions?.queryKey ?? hrListDepartmentsQueryKey(resolvedParams);

  const queryResult = useQuery(
    {
      ...hrListDepartmentsQueryOptions(resolvedParams, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<HrListDepartmentsStatus403 | HrListDepartmentsStatus422>
  > & { queryKey: TQueryKey };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
