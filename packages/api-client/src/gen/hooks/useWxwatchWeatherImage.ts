/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { wxwatchWeatherImage } from "../clients/wxwatchWeatherImage.js";
import type {
  WxwatchWeatherImageOptions,
  WxwatchWeatherImageResponse,
  WxwatchWeatherImageStatus422,
} from "../models/WxwatchWeatherImage.js";

export const wxwatchWeatherImageQueryKey = ({
  path,
}: Omit<WxwatchWeatherImageOptions, "headers">) =>
  [{ url: "/api/v1/wxwatch/images/:storage_path", params: path }] as const;

type WxwatchWeatherImageQueryKey = ReturnType<
  typeof wxwatchWeatherImageQueryKey
>;

export function wxwatchWeatherImageQueryOptions(
  { path }: WxwatchWeatherImageOptions,
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = wxwatchWeatherImageQueryKey({ path });
  return queryOptions<
    WxwatchWeatherImageResponse,
    ResponseErrorConfig<WxwatchWeatherImageStatus422>,
    WxwatchWeatherImageResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return wxwatchWeatherImage({
        ...config,
        path,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Issue a short-lived download redirect scoped to the environment's wxwatch prefix.
 * @summary Weather Image
 * {@link /api/v1/wxwatch/images/:storage_path}
 */
export function useWxwatchWeatherImage<
  TData = WxwatchWeatherImageResponse,
  TQueryData = WxwatchWeatherImageResponse,
  TQueryKey extends QueryKey = WxwatchWeatherImageQueryKey,
>(
  {
    path,
  }: {
    path:
      | WxwatchWeatherImageOptions["path"]
      | (() => WxwatchWeatherImageOptions["path"]);
  },
  options: {
    query?: Partial<
      QueryObserverOptions<
        WxwatchWeatherImageResponse,
        ResponseErrorConfig<WxwatchWeatherImageStatus422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const resolvedParams = { path: typeof path === "function" ? path() : path };
  const queryKey =
    resolvedOptions?.queryKey ?? wxwatchWeatherImageQueryKey(resolvedParams);

  const queryResult = useQuery(
    {
      ...wxwatchWeatherImageQueryOptions(resolvedParams, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<WxwatchWeatherImageStatus422>
  > & { queryKey: TQueryKey };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
