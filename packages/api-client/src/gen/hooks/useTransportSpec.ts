/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { transportSpec } from "../clients/transportSpec.js";
import type {
  TransportSpecStatus200,
  TransportSpecStatus422,
} from "../models/TransportSpec.js";

export const transportSpecQueryKey = () =>
  [{ url: "/api/v1/transport/spec" }] as const;

type TransportSpecQueryKey = ReturnType<typeof transportSpecQueryKey>;

export function transportSpecQueryOptions(
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = transportSpecQueryKey();
  return queryOptions<
    TransportSpecStatus200,
    ResponseErrorConfig<TransportSpecStatus422>,
    TransportSpecStatus200,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return transportSpec({
        ...config,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Deprecated: returns the v1 catalogue, which no longer changes. Use /transport/timetable/current, which follows published timetable versions.
 * @summary Get the transport timetable
 * @deprecated
 * {@link /api/v1/transport/spec}
 */
export function useTransportSpec<
  TData = TransportSpecStatus200,
  TQueryData = TransportSpecStatus200,
  TQueryKey extends QueryKey = TransportSpecQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        TransportSpecStatus200,
        ResponseErrorConfig<TransportSpecStatus422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? transportSpecQueryKey();

  const queryResult = useQuery(
    {
      ...transportSpecQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<TransportSpecStatus422>> & {
    queryKey: TQueryKey;
  };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
