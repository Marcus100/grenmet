/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { hrListAssignments } from "../clients/hrListAssignments.js";
import type {
  HrListAssignmentsOptions,
  HrListAssignmentsStatus200,
  HrListAssignmentsStatus400,
  HrListAssignmentsStatus403,
  HrListAssignmentsStatus404,
  HrListAssignmentsStatus422,
} from "../models/HrListAssignments.js";

export const hrListAssignmentsQueryKey = ({
  query,
}: Omit<HrListAssignmentsOptions, "headers">) =>
  [
    { url: "/api/v1/hr/rosters/assignments" },
    ...(query ? [query] : []),
  ] as const;

type HrListAssignmentsQueryKey = ReturnType<typeof hrListAssignmentsQueryKey>;

export function hrListAssignmentsQueryOptions(
  { query }: HrListAssignmentsOptions,
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = hrListAssignmentsQueryKey({ query });
  return queryOptions<
    HrListAssignmentsStatus200,
    ResponseErrorConfig<
      | HrListAssignmentsStatus400
      | HrListAssignmentsStatus403
      | HrListAssignmentsStatus404
      | HrListAssignmentsStatus422
    >,
    HrListAssignmentsStatus200,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return hrListAssignments({
        ...config,
        query,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Return rostered days between start and end, expanded to concrete local times, for the calendar. scope=me (the default) returns the caller's own assignments and needs no roster permission; scope=department returns the whole department's and requires roster.view. Draft periods are included only for callers with roster.manage. The range is capped at 92 days.
 * @summary List roster assignments for a date range
 * {@link /api/v1/hr/rosters/assignments}
 */
export function useHrListAssignments<
  TData = HrListAssignmentsStatus200,
  TQueryData = HrListAssignmentsStatus200,
  TQueryKey extends QueryKey = HrListAssignmentsQueryKey,
>(
  {
    query,
  }: {
    query:
      | HrListAssignmentsOptions["query"]
      | (() => HrListAssignmentsOptions["query"]);
  },
  options: {
    query?: Partial<
      QueryObserverOptions<
        HrListAssignmentsStatus200,
        ResponseErrorConfig<
          | HrListAssignmentsStatus400
          | HrListAssignmentsStatus403
          | HrListAssignmentsStatus404
          | HrListAssignmentsStatus422
        >,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const resolvedParams = {
    query: typeof query === "function" ? query() : query,
  };
  const queryKey =
    resolvedOptions?.queryKey ?? hrListAssignmentsQueryKey(resolvedParams);

  const queryResult = useQuery(
    {
      ...hrListAssignmentsQueryOptions(resolvedParams, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<
      | HrListAssignmentsStatus400
      | HrListAssignmentsStatus403
      | HrListAssignmentsStatus404
      | HrListAssignmentsStatus422
    >
  > & { queryKey: TQueryKey };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
