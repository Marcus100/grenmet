/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { authTwofaStatus } from "../clients/authTwofaStatus.js";
import type {
  AuthTwofaStatusStatus200,
  AuthTwofaStatusStatus422,
} from "../models/AuthTwofaStatus.js";

export const authTwofaStatusQueryKey = () =>
  [{ url: "/api/v1/2fa/status" }] as const;

type AuthTwofaStatusQueryKey = ReturnType<typeof authTwofaStatusQueryKey>;

export function authTwofaStatusQueryOptions(
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = authTwofaStatusQueryKey();
  return queryOptions<
    AuthTwofaStatusStatus200,
    ResponseErrorConfig<AuthTwofaStatusStatus422>,
    AuthTwofaStatusStatus200,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return authTwofaStatus({
        ...config,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Returns whether two-factor authentication is enabled for the current account.
 * @summary Get 2FA status
 * {@link /api/v1/2fa/status}
 */
export function useAuthTwofaStatus<
  TData = AuthTwofaStatusStatus200,
  TQueryData = AuthTwofaStatusStatus200,
  TQueryKey extends QueryKey = AuthTwofaStatusQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        AuthTwofaStatusStatus200,
        ResponseErrorConfig<AuthTwofaStatusStatus422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? authTwofaStatusQueryKey();

  const queryResult = useQuery(
    {
      ...authTwofaStatusQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<AuthTwofaStatusStatus422>> & {
    queryKey: TQueryKey;
  };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
