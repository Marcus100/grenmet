/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { capGetAlert } from "../clients/capGetAlert.js";
import type {
  CapGetAlertOptions,
  CapGetAlertStatus200,
  CapGetAlertStatus422,
} from "../models/CapGetAlert.js";

export const capGetAlertQueryKey = ({
  path,
}: Omit<CapGetAlertOptions, "headers">) =>
  [{ url: "/api/v1/cap/alerts/:alert_id", params: path }] as const;

type CapGetAlertQueryKey = ReturnType<typeof capGetAlertQueryKey>;

export function capGetAlertQueryOptions(
  { path }: CapGetAlertOptions,
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = capGetAlertQueryKey({ path });
  return queryOptions<
    CapGetAlertStatus200,
    ResponseErrorConfig<CapGetAlertStatus422>,
    CapGetAlertStatus200,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return capGetAlert({
        ...config,
        path,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Returns one CAP alert by its identifier when it is visible to the current user.
 * @summary Get a CAP alert
 * {@link /api/v1/cap/alerts/:alert_id}
 */
export function useCapGetAlert<
  TData = CapGetAlertStatus200,
  TQueryData = CapGetAlertStatus200,
  TQueryKey extends QueryKey = CapGetAlertQueryKey,
>(
  {
    path,
  }: { path: CapGetAlertOptions["path"] | (() => CapGetAlertOptions["path"]) },
  options: {
    query?: Partial<
      QueryObserverOptions<
        CapGetAlertStatus200,
        ResponseErrorConfig<CapGetAlertStatus422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const resolvedParams = { path: typeof path === "function" ? path() : path };
  const queryKey =
    resolvedOptions?.queryKey ?? capGetAlertQueryKey(resolvedParams);

  const queryResult = useQuery(
    {
      ...capGetAlertQueryOptions(resolvedParams, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<CapGetAlertStatus422>> & {
    queryKey: TQueryKey;
  };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
