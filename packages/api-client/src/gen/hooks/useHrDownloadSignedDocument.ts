/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { hrDownloadSignedDocument } from "../clients/hrDownloadSignedDocument.js";
import type {
  HrDownloadSignedDocumentOptions,
  HrDownloadSignedDocumentStatus200,
  HrDownloadSignedDocumentStatus403,
  HrDownloadSignedDocumentStatus404,
  HrDownloadSignedDocumentStatus422,
} from "../models/HrDownloadSignedDocument.js";

export const hrDownloadSignedDocumentQueryKey = ({
  path,
}: Omit<HrDownloadSignedDocumentOptions, "headers">) =>
  [
    { url: "/api/v1/hr/signed-documents/:document_id/pdf", params: path },
  ] as const;

type HrDownloadSignedDocumentQueryKey = ReturnType<
  typeof hrDownloadSignedDocumentQueryKey
>;

export function hrDownloadSignedDocumentQueryOptions(
  { path }: HrDownloadSignedDocumentOptions,
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = hrDownloadSignedDocumentQueryKey({ path });
  return queryOptions<
    HrDownloadSignedDocumentStatus200,
    ResponseErrorConfig<
      | HrDownloadSignedDocumentStatus403
      | HrDownloadSignedDocumentStatus404
      | HrDownloadSignedDocumentStatus422
    >,
    HrDownloadSignedDocumentStatus200,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return hrDownloadSignedDocument({
        ...config,
        path,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Returns the original PDF to its signer, subject, or an HR document reader with scope over the subject.
 * @summary Download a signed HR document
 * {@link /api/v1/hr/signed-documents/:document_id/pdf}
 */
export function useHrDownloadSignedDocument<
  TData = HrDownloadSignedDocumentStatus200,
  TQueryData = HrDownloadSignedDocumentStatus200,
  TQueryKey extends QueryKey = HrDownloadSignedDocumentQueryKey,
>(
  {
    path,
  }: {
    path:
      | HrDownloadSignedDocumentOptions["path"]
      | (() => HrDownloadSignedDocumentOptions["path"]);
  },
  options: {
    query?: Partial<
      QueryObserverOptions<
        HrDownloadSignedDocumentStatus200,
        ResponseErrorConfig<
          | HrDownloadSignedDocumentStatus403
          | HrDownloadSignedDocumentStatus404
          | HrDownloadSignedDocumentStatus422
        >,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const resolvedParams = { path: typeof path === "function" ? path() : path };
  const queryKey =
    resolvedOptions?.queryKey ??
    hrDownloadSignedDocumentQueryKey(resolvedParams);

  const queryResult = useQuery(
    {
      ...hrDownloadSignedDocumentQueryOptions(resolvedParams, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<
      | HrDownloadSignedDocumentStatus403
      | HrDownloadSignedDocumentStatus404
      | HrDownloadSignedDocumentStatus422
    >
  > & { queryKey: TQueryKey };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
