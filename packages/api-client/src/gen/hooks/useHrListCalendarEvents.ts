/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { hrListCalendarEvents } from "../clients/hrListCalendarEvents.js";
import type {
  HrListCalendarEventsOptions,
  HrListCalendarEventsStatus200,
  HrListCalendarEventsStatus400,
  HrListCalendarEventsStatus403,
  HrListCalendarEventsStatus404,
  HrListCalendarEventsStatus422,
} from "../models/HrListCalendarEvents.js";

export const hrListCalendarEventsQueryKey = ({
  query,
}: Omit<HrListCalendarEventsOptions, "headers">) =>
  [{ url: "/api/v1/hr/calendar/events" }, ...(query ? [query] : [])] as const;

type HrListCalendarEventsQueryKey = ReturnType<
  typeof hrListCalendarEventsQueryKey
>;

export function hrListCalendarEventsQueryOptions(
  { query }: HrListCalendarEventsOptions,
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = hrListCalendarEventsQueryKey({ query });
  return queryOptions<
    HrListCalendarEventsStatus200,
    ResponseErrorConfig<
      | HrListCalendarEventsStatus400
      | HrListCalendarEventsStatus403
      | HrListCalendarEventsStatus404
      | HrListCalendarEventsStatus422
    >,
    HrListCalendarEventsStatus200,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return hrListCalendarEvents({
        ...config,
        query,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Return the department's calendar entries overlapping the date range — meetings, training, inspections, visits, maintenance and anything else staff have recorded. The duty roster is a separate layer, read from /hr/rosters/assignments. Requires calendar.view; the range is capped at 92 days. Cancelled entries are omitted unless include_cancelled is set.
 * @summary List department calendar events
 * {@link /api/v1/hr/calendar/events}
 */
export function useHrListCalendarEvents<
  TData = HrListCalendarEventsStatus200,
  TQueryData = HrListCalendarEventsStatus200,
  TQueryKey extends QueryKey = HrListCalendarEventsQueryKey,
>(
  {
    query,
  }: {
    query:
      | HrListCalendarEventsOptions["query"]
      | (() => HrListCalendarEventsOptions["query"]);
  },
  options: {
    query?: Partial<
      QueryObserverOptions<
        HrListCalendarEventsStatus200,
        ResponseErrorConfig<
          | HrListCalendarEventsStatus400
          | HrListCalendarEventsStatus403
          | HrListCalendarEventsStatus404
          | HrListCalendarEventsStatus422
        >,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const resolvedParams = {
    query: typeof query === "function" ? query() : query,
  };
  const queryKey =
    resolvedOptions?.queryKey ?? hrListCalendarEventsQueryKey(resolvedParams);

  const queryResult = useQuery(
    {
      ...hrListCalendarEventsQueryOptions(resolvedParams, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<
      | HrListCalendarEventsStatus400
      | HrListCalendarEventsStatus403
      | HrListCalendarEventsStatus404
      | HrListCalendarEventsStatus422
    >
  > & { queryKey: TQueryKey };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
