/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { capGetRss } from "../clients/capGetRss.js";
import type {
  CapGetRssStatus200,
  CapGetRssStatus422,
} from "../models/CapGetRss.js";

export const capGetRssQueryKey = () => [{ url: "/api/cap/rss.xml" }] as const;

type CapGetRssQueryKey = ReturnType<typeof capGetRssQueryKey>;

export function capGetRssQueryOptions(
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = capGetRssQueryKey();
  return queryOptions<
    CapGetRssStatus200,
    ResponseErrorConfig<CapGetRssStatus422>,
    CapGetRssStatus200,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return capGetRss({
        ...config,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Returns active public CAP alerts as an RSS 2.0 feed for feed readers and aggregators.
 * @summary Get the public CAP RSS feed
 * {@link /api/cap/rss.xml}
 */
export function useCapGetRss<
  TData = CapGetRssStatus200,
  TQueryData = CapGetRssStatus200,
  TQueryKey extends QueryKey = CapGetRssQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        CapGetRssStatus200,
        ResponseErrorConfig<CapGetRssStatus422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? capGetRssQueryKey();

  const queryResult = useQuery(
    {
      ...capGetRssQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<CapGetRssStatus422>> & {
    queryKey: TQueryKey;
  };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
