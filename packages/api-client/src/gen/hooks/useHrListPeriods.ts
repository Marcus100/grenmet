/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { hrListPeriods } from "../clients/hrListPeriods.js";
import type {
  HrListPeriodsOptions,
  HrListPeriodsStatus200,
  HrListPeriodsStatus403,
  HrListPeriodsStatus422,
} from "../models/HrListPeriods.js";

export const hrListPeriodsQueryKey = ({
  query,
}: Omit<HrListPeriodsOptions, "headers">) =>
  [{ url: "/api/v1/hr/rosters/periods" }, ...(query ? [query] : [])] as const;

type HrListPeriodsQueryKey = ReturnType<typeof hrListPeriodsQueryKey>;

export function hrListPeriodsQueryOptions(
  { query }: HrListPeriodsOptions,
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = hrListPeriodsQueryKey({ query });
  return queryOptions<
    HrListPeriodsStatus200,
    ResponseErrorConfig<HrListPeriodsStatus403 | HrListPeriodsStatus422>,
    HrListPeriodsStatus200,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return hrListPeriods({
        ...config,
        query,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Return roster periods for a department, newest first. Requires roster.view permission.
 * @summary List roster periods
 * {@link /api/v1/hr/rosters/periods}
 */
export function useHrListPeriods<
  TData = HrListPeriodsStatus200,
  TQueryData = HrListPeriodsStatus200,
  TQueryKey extends QueryKey = HrListPeriodsQueryKey,
>(
  {
    query,
  }: {
    query:
      | HrListPeriodsOptions["query"]
      | (() => HrListPeriodsOptions["query"]);
  },
  options: {
    query?: Partial<
      QueryObserverOptions<
        HrListPeriodsStatus200,
        ResponseErrorConfig<HrListPeriodsStatus403 | HrListPeriodsStatus422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const resolvedParams = {
    query: typeof query === "function" ? query() : query,
  };
  const queryKey =
    resolvedOptions?.queryKey ?? hrListPeriodsQueryKey(resolvedParams);

  const queryResult = useQuery(
    {
      ...hrListPeriodsQueryOptions(resolvedParams, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<HrListPeriodsStatus403 | HrListPeriodsStatus422>
  > & { queryKey: TQueryKey };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
