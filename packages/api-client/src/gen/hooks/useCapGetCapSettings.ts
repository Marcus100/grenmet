/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { capGetCapSettings } from "../clients/capGetCapSettings.js";
import type {
  CapGetCapSettingsStatus200,
  CapGetCapSettingsStatus422,
} from "../models/CapGetCapSettings.js";

export const capGetCapSettingsQueryKey = () =>
  [{ url: "/api/v1/cap/settings" }] as const;

type CapGetCapSettingsQueryKey = ReturnType<typeof capGetCapSettingsQueryKey>;

export function capGetCapSettingsQueryOptions(
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = capGetCapSettingsQueryKey();
  return queryOptions<
    CapGetCapSettingsStatus200,
    ResponseErrorConfig<CapGetCapSettingsStatus422>,
    CapGetCapSettingsStatus200,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return capGetCapSettings({
        ...config,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Returns CAP publication and workflow settings visible to the current user.
 * @summary Get CAP settings
 * {@link /api/v1/cap/settings}
 */
export function useCapGetCapSettings<
  TData = CapGetCapSettingsStatus200,
  TQueryData = CapGetCapSettingsStatus200,
  TQueryKey extends QueryKey = CapGetCapSettingsQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        CapGetCapSettingsStatus200,
        ResponseErrorConfig<CapGetCapSettingsStatus422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? capGetCapSettingsQueryKey();

  const queryResult = useQuery(
    {
      ...capGetCapSettingsQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<CapGetCapSettingsStatus422>
  > & { queryKey: TQueryKey };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
