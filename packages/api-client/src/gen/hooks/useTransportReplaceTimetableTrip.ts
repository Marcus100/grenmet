/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  UseMutationOptions,
  UseMutationResult,
} from "@tanstack/react-query";
import { mutationOptions, useMutation } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { transportReplaceTimetableTrip } from "../clients/transportReplaceTimetableTrip.js";
import type {
  TransportReplaceTimetableTripOptions,
  TransportReplaceTimetableTripStatus200,
  TransportReplaceTimetableTripStatus403,
  TransportReplaceTimetableTripStatus404,
  TransportReplaceTimetableTripStatus409,
  TransportReplaceTimetableTripStatus422,
  TransportReplaceTimetableTripStatus503,
} from "../models/TransportReplaceTimetableTrip.js";

export const transportReplaceTimetableTripMutationKey = () =>
  [
    { url: "/api/v1/transport/timetable/versions/:version_id/trips/:trip_id" },
  ] as const;

export function transportReplaceTimetableTripMutationOptions<
  TContext = unknown,
>(
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const mutationKey = transportReplaceTimetableTripMutationKey();
  return mutationOptions<
    TransportReplaceTimetableTripStatus200,
    ResponseErrorConfig<
      | TransportReplaceTimetableTripStatus403
      | TransportReplaceTimetableTripStatus404
      | TransportReplaceTimetableTripStatus409
      | TransportReplaceTimetableTripStatus422
      | TransportReplaceTimetableTripStatus503
    >,
    TransportReplaceTimetableTripOptions,
    TContext
  >({
    mutationKey,
    mutationFn: async ({ path, body }) => {
      return transportReplaceTimetableTrip({
        ...config,
        path,
        body,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Replaces the trip's details and its full ordered stop list.
 * @summary Replace a trip in a draft
 * {@link /api/v1/transport/timetable/versions/:version_id/trips/:trip_id}
 */
export function useTransportReplaceTimetableTrip<TContext>(
  options: {
    mutation?: UseMutationOptions<
      TransportReplaceTimetableTripStatus200,
      ResponseErrorConfig<
        | TransportReplaceTimetableTripStatus403
        | TransportReplaceTimetableTripStatus404
        | TransportReplaceTimetableTripStatus409
        | TransportReplaceTimetableTripStatus422
        | TransportReplaceTimetableTripStatus503
      >,
      TransportReplaceTimetableTripOptions,
      TContext
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { mutation = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...mutationOptions } = mutation;
  const mutationKey =
    mutationOptions.mutationKey ?? transportReplaceTimetableTripMutationKey();

  const baseOptions = transportReplaceTimetableTripMutationOptions(
    config
  ) as UseMutationOptions<
    TransportReplaceTimetableTripStatus200,
    ResponseErrorConfig<
      | TransportReplaceTimetableTripStatus403
      | TransportReplaceTimetableTripStatus404
      | TransportReplaceTimetableTripStatus409
      | TransportReplaceTimetableTripStatus422
      | TransportReplaceTimetableTripStatus503
    >,
    TransportReplaceTimetableTripOptions,
    TContext
  >;

  return useMutation<
    TransportReplaceTimetableTripStatus200,
    ResponseErrorConfig<
      | TransportReplaceTimetableTripStatus403
      | TransportReplaceTimetableTripStatus404
      | TransportReplaceTimetableTripStatus409
      | TransportReplaceTimetableTripStatus422
      | TransportReplaceTimetableTripStatus503
    >,
    TransportReplaceTimetableTripOptions,
    TContext
  >(
    {
      ...baseOptions,
      mutationKey,
      ...mutationOptions,
    },
    queryClient
  ) as UseMutationResult<
    TransportReplaceTimetableTripStatus200,
    ResponseErrorConfig<
      | TransportReplaceTimetableTripStatus403
      | TransportReplaceTimetableTripStatus404
      | TransportReplaceTimetableTripStatus409
      | TransportReplaceTimetableTripStatus422
      | TransportReplaceTimetableTripStatus503
    >,
    TransportReplaceTimetableTripOptions,
    TContext
  >;
}
