/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { capGetFeeds } from "../clients/capGetFeeds.js";
import type {
  CapGetFeedsStatus200,
  CapGetFeedsStatus422,
} from "../models/CapGetFeeds.js";

export const capGetFeedsQueryKey = () =>
  [{ url: "/api/v1/cap/feeds" }] as const;

type CapGetFeedsQueryKey = ReturnType<typeof capGetFeedsQueryKey>;

export function capGetFeedsQueryOptions(
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = capGetFeedsQueryKey();
  return queryOptions<
    CapGetFeedsStatus200,
    ResponseErrorConfig<CapGetFeedsStatus422>,
    CapGetFeedsStatus200,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return capGetFeeds({
        ...config,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Returns configured external CAP feed sources available to the current user.
 * @summary List CAP feed sources
 * {@link /api/v1/cap/feeds}
 */
export function useCapGetFeeds<
  TData = CapGetFeedsStatus200,
  TQueryData = CapGetFeedsStatus200,
  TQueryKey extends QueryKey = CapGetFeedsQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        CapGetFeedsStatus200,
        ResponseErrorConfig<CapGetFeedsStatus422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? capGetFeedsQueryKey();

  const queryResult = useQuery(
    {
      ...capGetFeedsQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<CapGetFeedsStatus422>> & {
    queryKey: TQueryKey;
  };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
