/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { utilsHealthCheck } from "../clients/utilsHealthCheck.js";
import type {
  UtilsHealthCheckStatus200,
  UtilsHealthCheckStatus422,
} from "../models/UtilsHealthCheck.js";

export const utilsHealthCheckQueryKey = () =>
  [{ url: "/api/v1/utils/health-check/" }] as const;

type UtilsHealthCheckQueryKey = ReturnType<typeof utilsHealthCheckQueryKey>;

export function utilsHealthCheckQueryOptions(
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = utilsHealthCheckQueryKey();
  return queryOptions<
    UtilsHealthCheckStatus200,
    ResponseErrorConfig<UtilsHealthCheckStatus422>,
    UtilsHealthCheckStatus200,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return utilsHealthCheck({
        ...config,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Simple liveness probe; returns 200 if the process is running.
 * @summary Liveness
 * {@link /api/v1/utils/health-check/}
 */
export function useUtilsHealthCheck<
  TData = UtilsHealthCheckStatus200,
  TQueryData = UtilsHealthCheckStatus200,
  TQueryKey extends QueryKey = UtilsHealthCheckQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        UtilsHealthCheckStatus200,
        ResponseErrorConfig<UtilsHealthCheckStatus422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? utilsHealthCheckQueryKey();

  const queryResult = useQuery(
    {
      ...utilsHealthCheckQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<UtilsHealthCheckStatus422>> & {
    queryKey: TQueryKey;
  };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
