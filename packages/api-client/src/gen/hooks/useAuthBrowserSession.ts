/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import type { RequestConfig, ResponseErrorConfig } from "../.kubb/client.js";
import { authBrowserSession } from "../clients/authBrowserSession.js";
import type {
  AuthBrowserSessionStatus200,
  AuthBrowserSessionStatus422,
} from "../models/AuthBrowserSession.js";

export const authBrowserSessionQueryKey = () =>
  [{ url: "/api/v1/auth/browser/session" }] as const;

type AuthBrowserSessionQueryKey = ReturnType<typeof authBrowserSessionQueryKey>;

export function authBrowserSessionQueryOptions(
  config: Partial<
    Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
  > = {}
) {
  const queryKey = authBrowserSessionQueryKey();
  return queryOptions<
    AuthBrowserSessionStatus200,
    ResponseErrorConfig<AuthBrowserSessionStatus422>,
    AuthBrowserSessionStatus200,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return authBrowserSession({
        ...config,
        signal: config.signal ?? signal,
        throwOnError: true,
      }).unwrap();
    },
  });
}

/**
 * @description Validate the cookie and return a session-bound CSRF token, never the session secret.
 * @summary Browser Session
 * {@link /api/v1/auth/browser/session}
 */
export function useAuthBrowserSession<
  TData = AuthBrowserSessionStatus200,
  TQueryData = AuthBrowserSessionStatus200,
  TQueryKey extends QueryKey = AuthBrowserSessionQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        AuthBrowserSessionStatus200,
        ResponseErrorConfig<AuthBrowserSessionStatus422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<
      Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">
    >;
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? authBrowserSessionQueryKey();

  const queryResult = useQuery(
    {
      ...authBrowserSessionQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<
    TData,
    ResponseErrorConfig<AuthBrowserSessionStatus422>
  > & { queryKey: TQueryKey };

  queryResult.queryKey = queryKey as TQueryKey;

  return queryResult;
}
