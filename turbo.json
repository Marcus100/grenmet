{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV", "NEXT_PUBLIC_*"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "env": [
        "AUTH_APP_URL",
        "AUTH_API_URL",
        "AUTH_API_V1_STR",
        "SESSION_COOKIE_NAME",
        "SESSION_COOKIE_DOMAIN",
        "DB_URL",
        "DATABASE_URL",
        "RESEND_API_KEY"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "start": {
      "dependsOn": ["^build"],
      "persistent": true
    },
    "lint": {
      "inputs": ["**/*.{ts,tsx,js,jsx,mjs,cjs}", "biome.jsonc", "biome.json"]
    },
    "lint:fix": {
      "cache": false
    },
    "format": {
      "cache": false
    },
    "check": {
      "inputs": ["**/*.{ts,tsx,js,jsx,mjs,cjs}", "biome.jsonc", "biome.json"]
    },
    "check:fix": {
      "cache": false
    },
    "check:ci": {
      "inputs": ["**/*.{ts,tsx,js,jsx,mjs,cjs}", "biome.jsonc", "biome.json"]
    },
    "type-check": {
      "dependsOn": ["^build"],
      "inputs": ["**/*.{ts,tsx}", "**/tsconfig*.json"]
    },
    "generate": {
      "cache": false,
      "outputs": ["src/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"],
      "inputs": [
        "src/**",
        "tests/**",
        "**/*.test.{ts,tsx}",
        "**/*.spec.{ts,tsx}"
      ]
    }
  }
}
